# Camera Uploader Final

This is the fixed project with correct structure for Render deploy.

## Deploy on Render
- Build Command: `npm install`
- Start Command: `npm start`

Routes:
- `/` → Camera page
- `/upload` → Upload photo
- `/gallery` → View uploaded images
